# 项目规则

## Python 虚拟环境

- 虚拟环境目录：`.venv/`
- 已安装依赖：numpy, pandas, matplotlib, scipy
- 使用方式：
  ```powershell
  # 运行 Python 文件
  .venv\Scripts\python.exe <script>.py
  ```
- `.venv/` 已在 `.gitignore` 中忽略

## 统计学练习

- Python 验证脚本存放目录：`Inbox/`
- 运行前需激活虚拟环境
